sap.ui.define(["sap/ui/core/mvc/Controller"], function (Controller) {
  "use strict";

  return Controller.extend("com.dhenskript.defecttracker.controller.App", {
    onInit: function () {
      var that = this;
      // var oRouter = that.getOwnerComponent().getRouter();
      // if (sap.ui.getCore().signIn !== true) {
      //   oRouter.navTo("RouteSignIn");
      // }
    },
  });
});
